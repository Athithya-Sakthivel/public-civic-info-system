# Core AWS SDK (choose versions matched to each other)
boto3==1.40.61
botocore==1.40.61
s3transfer==0.14.0

# Database + vector store
psycopg[binary]==3.2.6
pgvector==0.4.2

# OpenSearch client
opensearch-py==3.1.0

# HTTP, utils
requests==2.32.5
python-dotenv==1.0.0
tqdm==4.67.1

# OCR / PDF
pytesseract==0.3.13
pdfminer.six==20250506
Pillow==11.3.0

# Small helpers commonly used in indexing pipelines
python-magic==0.4.27
filetype==1.2.0
fsspec==2025.12.0

# Minimal parsing / html utilities (lightweight)
lxml==5.4.0
beautifulsoup4==4.12.2
cssselect==1.3.0
trafilatura==2.0.0

# Embedding & model infra helpers (bedrock client calls use boto3)
pyyaml==6.0.3
jsonschema==4.26.0

# Optional but commonly used: ensure deterministic wheel builds for C libs
cffi==2.0.0
cryptography==46.0.3

# Lock file style metadata (not a package, used by tooling)
pip==23.2.1
setuptools==80.10.2
wheel==0.46.3
